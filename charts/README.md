Charts Service
==============

Scrapes, fetches, and sucks charts from various sources, caches them, and spits
out nicely formated XML/JSON for consumption.

Install dependencies:

    pip install -U -r requirements.txt
